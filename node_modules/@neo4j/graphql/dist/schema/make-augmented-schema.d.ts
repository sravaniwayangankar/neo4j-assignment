import { IResolvers, TypeSource } from "@graphql-tools/utils";
import { DocumentNode } from "graphql";
import { Neo4jGraphQLCallbacks } from "../types";
import { Node } from "../classes";
import Relationship from "../classes/Relationship";
declare function makeAugmentedSchema(typeDefs: TypeSource, { enableRegex, skipValidateTypeDefs, generateSubscriptions, callbacks, }?: {
    enableRegex?: boolean;
    skipValidateTypeDefs?: boolean;
    generateSubscriptions?: boolean;
    callbacks?: Neo4jGraphQLCallbacks;
}): {
    nodes: Node[];
    relationships: Relationship[];
    typeDefs: DocumentNode;
    resolvers: IResolvers;
};
export default makeAugmentedSchema;
//# sourceMappingURL=make-augmented-schema.d.ts.map