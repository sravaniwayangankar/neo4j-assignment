import { Node } from "../classes";
import { Context } from "../types";
import { CallbackBucket } from "../classes/CallbackBucket";
declare function createUpdateAndParams({ updateInput, varName, node, parentVar, chainStr, withVars, context, callbackBucket, parameterPrefix, includeRelationshipValidation, }: {
    parentVar: string;
    updateInput: any;
    varName: string;
    chainStr?: string;
    node: Node;
    withVars: string[];
    context: Context;
    callbackBucket: CallbackBucket;
    parameterPrefix: string;
    includeRelationshipValidation?: boolean;
}): [string, any];
export default createUpdateAndParams;
//# sourceMappingURL=create-update-and-params.d.ts.map