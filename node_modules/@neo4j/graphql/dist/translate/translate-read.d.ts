import { Node } from "../classes";
import { Context } from "../types";
declare function translateRead({ node, context, isRootConnectionField, }: {
    context: Context;
    node: Node;
    isRootConnectionField?: boolean;
}): [string, any];
export default translateRead;
//# sourceMappingURL=translate-read.d.ts.map